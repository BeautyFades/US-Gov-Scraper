import os

# TODO: proved I can use the existence of "K_SERVICE" or "K_REVISION" environment variable
# to check if running on GCR automatically. Implement that feature
ENVIRONMENT = 'dev-local' # Either dev-local, dev-cloudrun or prd-cloudrun.

